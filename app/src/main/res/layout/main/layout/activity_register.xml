<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".main.RegisterActivity"
    android:background="@drawable/ic_bg_register"
    android:orientation="vertical"
    android:layout_gravity="center_vertical"
    android:gravity="center">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="64dp"
        android:layout_marginBottom="64dp"
        android:orientation="vertical">

        <EditText
            android:id="@+id/register_account_edit_text"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="16dp"
            android:inputType="numberDecimal"
            android:maxLines="1"
            android:maxLength="11"
            android:background="@null"
            android:drawableBottom="@drawable/bg_text_line"
            android:textColor="@color/white_text"
            android:textColorHint="@color/hint_text"
            android:hint="手机号"/>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:orientation="horizontal">

            <EditText
                android:id="@+id/register_verify_code_edit_text"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="2"
                android:layout_marginRight="4dp"
                android:inputType="numberDecimal"
                android:maxLines="1"
                android:maxLength="4"
                android:background="@null"
                android:drawableBottom="@drawable/bg_text_line"
                android:textColor="@color/white_text"
                android:textColorHint="@color/hint_text"
                android:hint="请输入验证码"/>

            <Button
                android:id="@+id/register_get_code_btn"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:background="@drawable/bg_button"
                android:textColor="@color/white_text"
                android:text="获取验证码"/>

        </LinearLayout>

        <EditText
            android:id="@+id/register_password_edit_text"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="16dp"
            android:inputType="textPassword"
            android:maxLines="1"
            android:maxLength="18"
            android:background="@null"
            android:drawableBottom="@drawable/bg_text_line"
            android:textColor="@color/white_text"
            android:textColorHint="@color/hint_text"
            android:hint="密码"/>

        <Button
            android:id="@+id/register_btn"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="24dp"
            android:background="@drawable/bg_button"
            android:text="注                册"
            android:textColor="@color/white_text"
            android:textSize="17sp"/>


    </LinearLayout>



</LinearLayout>